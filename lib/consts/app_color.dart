import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xff8E6CEF);
  static const background = Color(0xff1D182A);
  static const secondBackground = Color(0xff342F3F);
  static const enabled = Color.fromRGBO(76, 76, 76, 1);
  static const transparent = Colors.transparent;
  static const white = Colors.white;

  // Text:
  static const disabled = Color.fromRGBO(0, 0, 0, 0.4);
  static const mediumEmphasis = Color.fromRGBO(0, 0, 0, 0.64);
  static const highEmphasis = Color.fromRGBO(0, 0, 0, 1);
}
